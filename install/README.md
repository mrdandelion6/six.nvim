# installation

you can do a quick install by just running `boostrap.sh` located in `install/` for the following linux distros:
- Arch
- Ubuntu
- Rocky

if you do not have sudo , then it will install things to `~/.local/` using curl. you will need to add `~/.local/bin` to PATH.
